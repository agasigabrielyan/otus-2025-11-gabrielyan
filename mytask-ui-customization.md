# Практика: Кастомизация интерфейсов Bitrix24 (OTUS)

> **Источник:** `Кастомизация_интерфейсов_2026-615949-d64180.pdf` (Roman Mendeleev)  
> **Проект:** `c:\OSPanel\domains\otusgabrielyan.loc` (коробка)  
> **Режим:** теория → практика → конспект для лекции

---

## Цели (из вебинара)

1. Назвать **6 способов** кастомизации интерфейса портала  
2. **Выбрать** подходящий способ в разной ситуации  
3. Внедрять новый функционал в Bitrix24

---

## 6 способов (зубрить)

| # | Способ | Когда выбирать | Риск при обновлении |
|---|--------|----------------|---------------------|
| 1 | **Встроенные настройки** | Поля, меню, тема, обязательность полей по стадии | Низкий |
| 2 | **Компоненты / шаблоны** | Глубокая переделка верстки, лента, сложная логика отображения | **Высокий** (кроме `result_modifier`) |
| 3 | **REST-приложения (placement)** | Пункт в CRM, вкладка в карточке, облако+коробка | Низкий (архитектурно) |
| 4 | **Отложенные функции** | Кнопка/блок в шапке страницы (`inside_pagetitle` и др.) | Средний (мало точек, могут меняться) |
| 5 | **Свои типы UF** | Виджет/верстка в карточке CRM (не обязательно «настоящее» поле) | Средний |
| 6 | **JavaScript (клиент)** | Сайдбар задач, динамический DOM, реакция на BX-события | **Высокий** (верстка меняется) |

### Шпаргалка «задача → способ» (из PDF)

| Задача | Ответ |
|--------|--------|
| Приоритет в правом сайдбаре задач | **JS** |
| Объём «Мой диск» после заголовка | **Отложенные функции** |
| Пункт «Передать в 1С» в меню сделки | **REST** или **JS** |
| Скрыть посты в ленте для группы | **Шаблон** или **JS** |
| Обязательное поле «Договор» на стадии | **Настройка** |
| Виджет статуса рабочего дня | **UF-тип** / шаблон / **JS** |

---

## Карта практики в проекте

```
local/
├── init.php                              ← OnProlog → подключение практики
└── practice/ui-customization/
    ├── deffered_demo.php                 ← Шаг 2 (готово)
    ├── js_extension/                     ← Шаг 3 (следующий)
    └── README.md                         ← заметки
```

---

## Пошаговый план

### Шаг 0 — Обзор ✅
- [x] Прочитать 6 способов (таблица выше)
- [x] Пройти шпаргалку «задача → способ»

### Шаг 1 — Встроенные настройки (теория) ✅
- [x] Меню, поля карточек, горизонтальное меню, логотип, тема
- [x] Пример: обязательное поле на стадии CRM — без кода

### Шаг 2 — Отложенные функции ✅
- [x] `deffered_demo.php` + `AddViewContent('inside_pagetitle')`
- [x] Подключение через `OnProlog` в `local/init.php`

**Точки встройки (зубрить):** `above_pagetitle`, `below_pagetitle`, `sidebar`, `topblock`, `pagetitle`, `inside_pagetitle`  
**Как найти:** поиск `ShowViewContent` по сайту

### Шаг 3 — JavaScript / JS-расширение ✅
- [x] `js/script.js`, `js_ext_demo.php`
- [x] `CJSCore::RegisterExt` + `Init`
- [x] `BX.ready` + alert (минимальный пример)

### Шаг 4 — REST placement ✅
- [x] `rest/placement_handler.php` — страница в iframe
- [x] `rest/placement_bind_example.php` — пример `placement.bind`
- [ ] **Практика (опционально):** локальное REST-приложение + bind (если есть CRM)

### Шаг 5 — Свой тип пользовательского поля ⏭️
- [x] Код в проекте (`uf_type/`) — для отдельного урока
- [ ] Отложено: полный разбор в следующей лекции по кастомным полям

### Шаг 6 — Компоненты / result_modifier ⏭️
- [x] Тема известна — пропущено

### Шаг 7 — Итог, выбор способа 🔄
- [ ] Самопроверка по таблице «задача → способ»
- [ ] Алгоритм выбора способа (дерево решений)
- [ ] Конспект лекции на EN (опционально)

---

## Текущий статус

> **СТОП:** шаг 7 — итог, выбор способа. Курс UI-кастомизации завершаем здесь.

| Шаг | Статус |
|-----|--------|
| 0–1 | ✅ теория |
| 2–3 | ✅ код в проекте |
| 4 | ✅ теория + handler |
| 5 | ⏭️ отдельный урок |
| 6 | ⏭️ знакомо |
| 7 | 🔄 итог |

---

## Ссылки (из PDF)

- Кастомизация интерфейса (dev.1c-bitrix.ru)
- BX.addCustomEvent
- B24 Apps / REST placements
- Отложенные функции
- Свой тип пользовательского поля
- result_modifier
